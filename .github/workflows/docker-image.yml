name: Build & Publish images

on:
  release:
    types: [published]
  workflow_dispatch:

jobs:
  hello-world:
    name: Build & Publish
    runs-on: ubuntu-latest
    steps:
    - name: Build & Publish
      run: python scripts/build_and_publish.py
      shell: sh
